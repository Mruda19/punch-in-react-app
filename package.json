{
  "name": "punch-in-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "couchbase": "^4.3.0",
    "body-parser": "^1.20.2"
  }
}